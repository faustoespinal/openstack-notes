apiVersion: v1
kind: Pod
metadata:
  name: busybox
  namespace: default
spec:
  containers:
  - image: busybox
    command: 
      - "/bin/sh"
    args: ["-c", "set COUNT=1; while true; do echo 'hello there counter: $COUNT'; set COUNT=`expr $COUNT + 1`; sleep 1;done"]
    imagePullPolicy: IfNotPresent
    name: busybox
  restartPolicy: Always
